dataContractSpecification: 1.1.0
id: urn:datacontract:jde:e1:f0411
info:
  title: JD Edwards EnterpriseOne F0411
  version: 1.1.0
  description: 'Supplier Ledger (JD Edwards EnterpriseOne 9.2). Values are stored in RAW JDE encoding:
    julian dates and implied-decimal integers are NOT dates and decimals in storage. SAP counterpart:
    ACDOCA where koart = K.'
  owner: Finance Data Engineering Team
models:
  f0411:
    type: table
    fields:
      rpkco:
        type: string
        description: 'Document Company. Part of the JDE primary key. SAP counterpart: RBUKRS. Physical
          type in the lakehouse: varchar.'
        primary_key: true
        required: true
      rpdoc:
        type: integer
        description: 'Document Number. Part of the JDE primary key. SAP counterpart: BELNR. Physical type
          in the lakehouse: bigint.'
        primary_key: true
        required: true
      rpdct:
        type: string
        description: 'Document Type. Part of the JDE primary key. SAP counterpart: BLART. Physical type
          in the lakehouse: varchar.'
        primary_key: true
        required: true
      rpsfx:
        type: string
        description: 'Document Pay Item. Part of the JDE primary key. SAP counterpart: DOCLN. Physical
          type in the lakehouse: varchar.'
        primary_key: true
        required: true
      rpan8:
        type: integer
        description: 'Address Number. SAP counterpart: LIFNR. Physical type in the lakehouse: bigint.'
      rpco:
        type: string
        description: 'Company. SAP counterpart: RBUKRS. Physical type in the lakehouse: varchar.'
      rpmcu:
        type: string
        description: 'Business Unit. SAP counterpart: RCNTR. Physical type in the lakehouse: varchar.'
      rpaid:
        type: string
        description: 'Account ID. SAP counterpart: RACCT. Physical type in the lakehouse: varchar.'
      rpglc:
        type: string
        description: 'G/L Offset. JDE-only; JDE AAI offset. Physical type in the lakehouse: varchar.'
      rpag:
        type: integer
        description: 'Gross Amount. Monetary amount stored RAW as an integer with 2 implied decimal places
          (divide by 10^2 to get the real value). Not a decimal in storage; a model that unions it against
          a true DECIMAL must cast it. SAP counterpart: HSL. Physical type in the lakehouse: bigint.'
      rpaap:
        type: integer
        description: 'Open Amount. Monetary amount stored RAW as an integer with 2 implied decimal places
          (divide by 10^2 to get the real value). Not a decimal in storage; a model that unions it against
          a true DECIMAL must cast it. JDE-only; JDE tracks open amount on the row. Physical type in the
          lakehouse: bigint.'
      rpadsc:
        type: integer
        description: 'Discount Available. Monetary amount stored RAW as an integer with 2 implied decimal
          places (divide by 10^2 to get the real value). Not a decimal in storage; a model that unions
          it against a true DECIMAL must cast it. JDE-only; JDE discount available. Physical type in the
          lakehouse: bigint.'
      rpacr:
        type: integer
        description: 'Foreign Gross Amount. Monetary amount stored RAW as an integer with 2 implied decimal
          places (divide by 10^2 to get the real value). Not a decimal in storage; a model that unions
          it against a true DECIMAL must cast it. SAP counterpart: WSL. Physical type in the lakehouse:
          bigint.'
      rpcrcd:
        type: string
        description: 'Currency Code. SAP counterpart: RTCUR. Physical type in the lakehouse: varchar.'
      rpdivj:
        type: integer
        description: 'Invoice Date. Stored RAW as a JDE julian CYYDDD integer (e.g. 124001 = 2024-01-01),
          NOT a date. Decode downstream; do not compare or union it against a date or a string. SAP counterpart:
          BLDAT. Physical type in the lakehouse: integer.'
      rpdgj:
        type: integer
        description: 'G/L Date. Stored RAW as a JDE julian CYYDDD integer (e.g. 124001 = 2024-01-01),
          NOT a date. Decode downstream; do not compare or union it against a date or a string. SAP counterpart:
          BUDAT. Physical type in the lakehouse: integer.'
      rpddj:
        type: integer
        description: 'Due Date. Stored RAW as a JDE julian CYYDDD integer (e.g. 124001 = 2024-01-01),
          NOT a date. Decode downstream; do not compare or union it against a date or a string. JDE-only;
          JDE due date. Physical type in the lakehouse: integer.'
      rppst:
        type: string
        description: 'Pay Status. JDE-only; pay status drives the clearing update. Physical type in the
          lakehouse: varchar.'
      rpdmtj:
        type: integer
        description: 'Date Paid. Stored RAW as a JDE julian CYYDDD integer (e.g. 124001 = 2024-01-01),
          NOT a date. Decode downstream; do not compare or union it against a date or a string. SAP counterpart:
          AUGDT. Physical type in the lakehouse: integer.'
      rpcknu:
        type: integer
        description: 'Matching Document Number. SAP counterpart: AUGBL. Physical type in the lakehouse:
          bigint.'
      rpfy:
        type: integer
        description: 'Fiscal Year. SAP counterpart: GJAHR. Physical type in the lakehouse: bigint.'
      rpctry:
        type: integer
        description: 'Century. JDE-only; JDE splits century from year. Physical type in the lakehouse:
          bigint.'
      rpicu:
        type: integer
        description: 'Batch Number. SAP counterpart: AWREF. Physical type in the lakehouse: bigint.'
      rpicut:
        type: string
        description: 'Batch Type. SAP counterpart: AWTYP. Physical type in the lakehouse: varchar.'
      rppost:
        type: string
        description: 'Posted Code. JDE-only; drives the post update. Physical type in the lakehouse: varchar.'
      rpvinv:
        type: string
        description: 'Supplier Invoice Number. SAP counterpart: XBLNR. Physical type in the lakehouse:
          varchar.'
      rprmk:
        type: string
        description: 'Remark. SAP counterpart: SGTXT. Physical type in the lakehouse: varchar.'
      rpuser:
        type: string
        description: 'User ID. SAP counterpart: USNAM. Physical type in the lakehouse: varchar.'
      rppid:
        type: string
        description: 'Program ID. SAP counterpart: TCODE. Physical type in the lakehouse: varchar.'
      rpjobn:
        type: string
        description: 'Work Station ID. JDE-only; JDE audit column. Physical type in the lakehouse: varchar.'
      rpupmj:
        type: integer
        description: 'Date Updated. Stored RAW as a JDE julian CYYDDD integer (e.g. 124001 = 2024-01-01),
          NOT a date. Decode downstream; do not compare or union it against a date or a string. SAP counterpart:
          AEDAT. Physical type in the lakehouse: integer.'
      rpupmt:
        type: integer
        description: 'Time Updated. Stored RAW as a JDE HHMMSS integer, not a time. JDE-only; JDE audit
          column. Physical type in the lakehouse: integer.'
      __op:
        type: string
        description: 'CDC operation flag: I / U / D. Rows with ''D'' are deletions. Physical type in the
          lakehouse: varchar.'
      __ts:
        type: integer
        description: 'CDC change sequence, monotonic per key. The version column for change-log dedup.
          Physical type in the lakehouse: bigint.'
